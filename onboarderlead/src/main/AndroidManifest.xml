<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.booboomx.onboarderlead">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".onboarder.activity.ListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity android:name=".onboarder.AhoyOnboarderActivity"/> -->

        <activity android:name=".onboarder.activity.GradientBackgroundExampleActivity" />
        <activity android:name=".onboarder.activity.ImageBackgroundExampleActivity" />
        <activity android:name=".onboarder.activity.SolidBackgroundExampleActivity"></activity>
    </application>

</manifest>